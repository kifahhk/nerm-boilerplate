import React from 'react';
import { shallow, expect } from '../../../util/test-helper';
import {{cNameClassName}} from '../{{cName}}';

describe('{{cNameClassName}}', () => {
  let component;
  const props = {
    className: 'custom-class',
  };

  beforeEach(() => {
    component = shallow(<{{cNameClassName}} {...props} />);
  });

  it('should render correctly', () =>
    expect(component.hasClass('custom-class')).be.true
  );
});
