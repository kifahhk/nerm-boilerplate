import {{modelNameClassName}} from '../models/{{modelName}}';

export function create{{modelNameClassName}}(req, res) {
  const new{{modelNameClassName}} = new {{modelNameClassName}}(req.body.{{modelNameCamelCase}});
  new{{modelNameClassName}}.save((err, saved) => {
    if (err) {
      res.status(500).send(err);
    }
    res.json({ {{modelNameCamelCase}}: saved });
  });
}
