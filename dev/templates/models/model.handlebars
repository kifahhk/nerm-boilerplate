import mongoose from 'mongoose';
const Schema = mongoose.Schema;

const {{modelNameCamelCase}}Schema = new Schema({
  'created-date': { type: 'Date', default: Date.now, required: true },
});

export default mongoose.model('{{modelNameClassName}}', {{modelNameCamelCase}}Schema);
