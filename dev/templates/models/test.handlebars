import { expect } from 'chai';
import request from 'supertest';
import app from '../server';

describe('{{modelNameClassName}}', () => {
  it('should be able to create a new {{modelNameClassName}} when post to /api/{{modelName}}', (done) => {
    request(app)
      .post('/api/{{modelName}}')
      .send({ {{modelNameCamelCase}}: {} })
      .end((err, res) => {
        expect(res.body.{{modelNameCamelCase}}._id).to.exist;
        done();
      });
  });
});
