import { Router } from 'express';
import * as {{modelNameClassName}}Controller from '../controllers/{{modelName}}.controller';
const router = new Router();

// create new {{modelNameClassName}}
router.post('/{{modelName}}', {{modelNameClassName}}Controller.create{{modelNameClassName}});

export default router;
