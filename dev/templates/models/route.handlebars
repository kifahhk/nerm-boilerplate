import { Router } from 'express';
import * as {{modelNameClassName}}Controller from '../controllers/{{modelName}}.controller';
const router = new Router();

// test {{modelNameClassName}}
router.route('/{{modelName}}/test').get({{modelNameClassName}}Controller.test);

export default router;
