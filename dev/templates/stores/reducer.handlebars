import * as types from './{{storeName}}-type';

export const initialState = [];

export default function {{storeName}}(state = initialState, action) {
  switch (action.type) {
    case types.TEST:
      return [
        ...state,
      ];

    default:
      return state;
  }
}
