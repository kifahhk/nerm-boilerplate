import * as types from './{{storeName}}-type';

const initialState = [];

export default function todos(state = initialState, action) {
  switch (action.type) {
    case types.TEST:
      return [
        ...state,
      ];

    default:
      return state;
  }
}
